# Kernel configuration fragment for Odroid HC4
# This file contains kernel config options that override the default configuration

# Enable ZSTD as default compression for ZRAM
# ZSTD provides better compression ratios and performance than LZO
CONFIG_ZRAM_DEF_COMP_ZSTD=y
CONFIG_ZRAM_DEF_COMP="zstd"

# Ensure ZSTD backend is enabled
CONFIG_ZRAM_BACKEND_ZSTD=y

# Keep other ZRAM options
CONFIG_ZRAM=m
CONFIG_ZRAM_WRITEBACK=y
CONFIG_ZRAM_MEMORY_TRACKING=y
